# Tensor lifetime GIF

```sh
python3 tensor_lifetime.py --poll-ms 1 --frame-ms 900 \
  --trace /tmp/tensor-trace.csv --output test/tensor_lifecycle.gif \
  --launch -- /mnt/fileserver/prj/dmctp/user/venv/bin/lifecycle_demo
```
